// Make sure to replace the USERNAME!
export const username = 'your.email@example.com';

// Make sure to replace the PASSWORD!
export const password = 'password12345';

// Passing `undefined` will save to the default location:
// `~/.lastpass-vault-${USERNAME}`
export const path = undefined;
